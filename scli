#!/usr/bin/env python
"""
SCLI - CLI tool for executing selectable scripts with proper argument passing.

Usage:
    scli                                    # Interactive mode
    scli SCRIPT [ARGS...]                   # Run script with arguments
    scli -s SCRIPT [ARGS...]                # Run script with arguments (alternative)
    scli --help                             # Show this help

Examples:
    scli code_formatter --list-actions
    scli code_quality_checker --mode all --yes
    scli -s code_formatter --mode all --yes --no-verbose
"""

import sys
from pathlib import Path

# Add src to path
src_path = Path(__file__).parent / 'src'
sys.path.insert(0, str(src_path))

from scli_runner import main

if __name__ == "__main__":
    main()