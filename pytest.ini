[tool:pytest]
python_functions = test_*
python_classes = Test*
addopts = 
    -v
    --tb=short
    --color=yes
    --durations=10
markers =
    integration: Integration tests - modules working together
    e2e: End-to-end tests - complete user workflows  
    slow: Slow tests (excluded during development)